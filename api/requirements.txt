pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
# seaborn is excluded (pulls scipy)
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
pydantic>=2.0.0
jinja2>=3.1.2
openai>=1.0.0
tabulate>=0.9.0
# scipy is excluded (too large)
# plotly is excluded (too large)
# xhtml2pdf is excluded (system dependencies)
